deployment:
  replicas: 3

ingressRoute:
  dashboard:
    enabled: true
    annotations: {}
    labels: {}
    matchRule: Host(`test.sugjo.dev`)
    entryPoints: ["local"]
    middlewares: []
    tls:
      secretName: sugjodev

  healthcheck:
    enabled: false
    annotations: {}
    labels: {}
    matchRule: PathPrefix(`/ping`)
    entryPoints: ["traefik"]
    middlewares: []
    tls:
      secretName: sugjodev

  rancher:
    enabled: true
    labels: {}
    matchRule: Host(`test.sugjo.dev`)
    service: rancher
    entryPoints: ["local"]
    middlewares: []
    tls:
      secretName: sugjodev

globalArguments:
- "--global.checknewversion"

ports:

  local:
    port: 8443
    expose: true
    exposedPort: 443
    middlewares: []
    tls:
      secretName: sugjodev

  web:
    redirectTo:
      port: websecure

  websecure:
    port: 8444
    expose: true
    exposedPort: 444
    middlewares: []
    tls:
      secretName: sugjodev

logs:
  general:
    level: ERROR