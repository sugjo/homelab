apiVersion: apps/v1
kind: Deployment
metadata:
  name: webserver
  labels:
    app: webserver
spec:
  selector:
    matchLabels:
      app: webserver
  replicas: 1
  template:
    metadata:
      labels:
        app: webserver
    spec:
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: nginx
        image: nginx:latest
        ports:
          - name: web
            containerPort: 80
        volumeMounts:
          - name: webserver-cm
            mountPath: /etc/nginx
          - name: local
            mountPath: /usr/share/nginx/html
      volumes:
      - name: webserver-cm
        configMap:
          name: webserver-cm
      - name: local
        persistentVolumeClaim:
          claimName: nfs
