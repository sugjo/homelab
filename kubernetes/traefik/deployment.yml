apiVersion: v1
kind: ServiceAccount
metadata:
  name: traefik-ingress-controller

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik
  labels:
    app: traefik

spec:
  replicas: 1
  selector:
    matchLabels:
      app: traefik
  template:
    metadata:
      labels:
        app: traefik
    spec:
      serviceAccountName: traefik-ingress-controller
      containers:
        - name: traefik
          image: traefik:v2.11
          args:
            - --api.insecure
            - --entryPoints.local.address=:443
            - --entryPoints.websecure.address=:444
            - --providers.kubernetesingress
            # - --serversTransport.insecureSkipVerify=true
          ports:
            - name: local
              containerPort: 443
            - name: websecure
              containerPort: 443
            - name: dashboard
              containerPort: 8080
